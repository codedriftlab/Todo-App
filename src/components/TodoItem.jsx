function  TodoItem() {
    return (
        <div>
            /* This is the TodoItem component. You can add your code here to create a single todo item. For example, you can display the todo text, add a checkbox to mark it as completed, and include a delete button to remove the item from the list. Make sure to style the component using CSS to make it visually appealing. */
            
        </div>
    );
}

export default TodoItem;  
